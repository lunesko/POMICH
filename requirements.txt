# Pinned for reproducible production builds (2026-09-21 audit).
# Bump intentionally; avoid unpinned >= ranges on the deploy path.
fastapi==0.115.12
uvicorn[standard]==0.34.2
flask==3.1.0
pydantic==2.11.3
pytest==8.3.5
httpx==0.28.1
sqlalchemy==2.0.40
psycopg[binary]==3.2.6
geoalchemy2==0.17.1
cryptography==44.0.2
argon2-cffi==23.1.0
