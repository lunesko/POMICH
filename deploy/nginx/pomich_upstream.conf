# Shared upstream for pomich.help (http context — /etc/nginx/conf.d/).
upstream pomich_app {
    server 127.0.0.1:8000;
    keepalive 16;
}

map $http_upgrade $connection_upgrade {
    default upgrade;
    '' "";
}
